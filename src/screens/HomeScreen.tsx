import React from 'react';
import { Pressable, StyleSheet, Text, View } from 'react-native';
import { ArrowRight, BookOpen, Building2, Coffee, Flag, Headphones, Lock, MessageCircle, Play, RotateCcw, TrainFront, Utensils, BriefcaseBusiness, Landmark, House, Stethoscope } from 'lucide-react-native';
import { BottomTabScreenProps } from '@react-navigation/bottom-tabs';
import { NativeStackNavigationProp } from '@react-navigation/native-stack';
import { DemoBanner } from '../components/DemoBanner';
import { EagleMark } from '../components/BrandMark';
import { ProgressBar } from '../components/ProgressBar';
import { Screen } from '../components/Screen';
import { SectionHeader } from '../components/SectionHeader';
import { Button } from '../components/Button';
import { colors, radii, spacing, typography } from '../constants/theme';
import { beltById } from '../constants/belts';
import { starterMissions } from '../data/lessons';
import { useAuth } from '../contexts/AuthContext';
import type { MainTabParamList, RootStackParamList } from '../navigation/types';

type Props = BottomTabScreenProps<MainTabParamList, 'Home'>;
const missionIcons: Record<string, any> = { Headphones, MessageCircle, BookOpen, Flag, RotateCcw };
const cityAreas = [
  { name: 'Café', sub: 'Ein Kaffee, bitte', icon: Coffee, unlocked: true }, { name: 'Bahnhof', sub: 'Nächster Halt', icon: TrainFront, unlocked: true }, { name: 'Markt', sub: 'Einkaufen', icon: Building2, unlocked: false }, { name: 'Restaurant', sub: 'Guten Appetit', icon: Utensils, unlocked: false }, { name: 'Arbeit', sub: 'Im Büro', icon: BriefcaseBusiness, unlocked: false }, { name: 'Bank', sub: 'Finanzen', icon: Landmark, unlocked: false }, { name: 'Wohnung', sub: 'Zu Hause', icon: House, unlocked: false }, { name: 'Arzt', sub: 'Gesundheit', icon: Stethoscope, unlocked: false },
];

export function HomeScreen({ navigation }: Props) {
  const { profile, demoMode } = useAuth();
  const belt = beltById(profile?.current_belt ?? 'white');
  const rootNavigation = navigation.getParent<NativeStackNavigationProp<RootStackParamList>>();
  const beltProgress = Math.min((profile?.xp ?? 0) / 400, 1);
  return <Screen><View style={styles.header}><View><Text style={styles.eyebrow}>QUARTA-FEIRA · 12 JUN</Text><Text style={styles.greeting}>Guten Tag, {profile?.name ?? 'Lernende'}!</Text></View><View style={styles.avatar}><Text style={styles.avatarText}>{(profile?.name ?? 'D').slice(0, 1).toUpperCase()}</Text></View></View>{demoMode && <DemoBanner />}<View style={styles.heroCard}><View style={styles.heroTop}><View><Text style={styles.cardEyebrow}>SUA FAIXA ATUAL</Text><Text style={styles.beltName}>{belt.name}</Text><Text style={styles.beltTitle}>{belt.cefr} · {belt.title}</Text></View><View style={[styles.beltPill, { backgroundColor: belt.color }]}><Text style={[styles.beltPillText, { color: belt.id === 'white' ? colors.background : colors.white }]}>{belt.cefr}</Text></View></View><View style={styles.progressMeta}><Text style={styles.progressLabel}>Progresso da faixa</Text><Text style={styles.progressValue}>{Math.round(beltProgress * 100)}%</Text></View><ProgressBar value={beltProgress} color={colors.goldBright} /><View style={styles.heroStats}><View><Text style={styles.statValue}>{profile?.xp ?? 0}</Text><Text style={styles.statLabel}>XP TOTAL</Text></View><View><Text style={styles.statValue}>{profile?.streak ?? 0}<Text style={styles.statSuffix}> dias</Text></Text><Text style={styles.statLabel}>SEQUÊNCIA</Text></View><EagleMark size={58} /></View></View><Button label="Continuar aprendendo" onPress={() => rootNavigation?.navigate('Lesson', { lessonId: 'lesson-1' })} icon={<Play size={18} color={colors.background} fill={colors.background} />} /><SectionHeader title="Sua cidade" action="Mapa" /><Text style={styles.sectionCopy}>Cada lugar desbloqueia um alemão mais real.</Text><View style={styles.cityGrid}>{cityAreas.map((area) => { const Icon = area.icon; return <Pressable key={area.name} disabled={!area.unlocked} onPress={() => rootNavigation?.navigate('Lesson', { lessonId: 'lesson-1' })} style={[styles.cityCard, !area.unlocked && styles.locked]}><View style={[styles.cityIcon, area.unlocked && styles.cityIconActive]}><Icon size={19} color={area.unlocked ? colors.gold : colors.textFaint} /></View><Text style={styles.cityName}>{area.name}</Text><Text style={styles.citySub}>{area.sub}</Text>{!area.unlocked && <Lock size={13} color={colors.textFaint} style={styles.lock} />}</Pressable>; })}</View><SectionHeader title="Missões de hoje" action="Ver todas" /><View style={styles.missions}>{starterMissions.slice(0, 2).map((mission) => { const Icon = missionIcons[mission.icon] ?? Flag; return <View key={mission.id} style={styles.mission}><View style={styles.missionIcon}><Icon size={17} color={colors.gold} /></View><View style={styles.missionBody}><Text style={styles.missionTitle}>{mission.title}</Text><ProgressBar value={mission.progress / mission.target} height={6} /><Text style={styles.missionProgress}>{mission.progress}/{mission.target} · +{mission.xpReward} XP</Text></View>{mission.completed && <Text style={styles.done}>✓</Text>}</View>; })}</View><View style={styles.quote}><Text style={styles.quoteMark}>“</Text><Text style={styles.quoteText}>Jede Reise beginnt mit einem ersten Schritt.</Text><Text style={styles.quoteTranslation}>Toda jornada começa com um primeiro passo.</Text></View></Screen>;
}

const styles = StyleSheet.create({ header: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', marginBottom: spacing.lg }, eyebrow: { ...typography.caption, color: colors.textFaint }, greeting: { ...typography.h1, color: colors.text, marginTop: 5 }, avatar: { width: 44, height: 44, borderRadius: 22, alignItems: 'center', justifyContent: 'center', backgroundColor: colors.red, borderWidth: 2, borderColor: colors.redBright }, avatarText: { ...typography.h2, color: colors.text }, heroCard: { backgroundColor: colors.surface, borderRadius: radii.lg, padding: spacing.lg, borderWidth: 1, borderColor: colors.border, marginBottom: spacing.md }, heroTop: { flexDirection: 'row', justifyContent: 'space-between' }, cardEyebrow: { ...typography.caption, color: colors.textFaint }, beltName: { ...typography.h2, color: colors.text, marginTop: 6 }, beltTitle: { ...typography.body, color: colors.textMuted, marginTop: 3 }, beltPill: { width: 50, height: 50, borderRadius: 25, alignItems: 'center', justifyContent: 'center' }, beltPillText: { fontWeight: '900', fontSize: 14 }, progressMeta: { flexDirection: 'row', justifyContent: 'space-between', marginTop: spacing.lg, marginBottom: 8 }, progressLabel: { ...typography.caption, color: colors.textMuted, textTransform: 'uppercase' }, progressValue: { ...typography.caption, color: colors.gold }, heroStats: { flexDirection: 'row', alignItems: 'flex-end', gap: spacing.xl, marginTop: spacing.lg }, statValue: { ...typography.h1, color: colors.goldBright }, statSuffix: { ...typography.caption, color: colors.gold, fontSize: 11 }, statLabel: { ...typography.caption, color: colors.textFaint, marginTop: 2 }, sectionCopy: { ...typography.body, color: colors.textMuted, marginTop: -6, marginBottom: spacing.md }, cityGrid: { flexDirection: 'row', flexWrap: 'wrap', gap: spacing.sm, marginBottom: spacing.xl }, cityCard: { width: '31.8%', minHeight: 112, backgroundColor: colors.surface, borderRadius: radii.md, padding: spacing.sm, borderWidth: 1, borderColor: colors.border }, locked: { opacity: 0.56 }, cityIcon: { width: 34, height: 34, borderRadius: 17, backgroundColor: colors.surfaceElevated, alignItems: 'center', justifyContent: 'center', marginBottom: 9 }, cityIconActive: { backgroundColor: '#382C15' }, cityName: { ...typography.body, color: colors.text, fontWeight: '800', fontSize: 13 }, citySub: { ...typography.caption, color: colors.textFaint, marginTop: 3, letterSpacing: 0, fontSize: 10 }, lock: { position: 'absolute', top: 10, right: 9 }, missions: { gap: spacing.sm }, mission: { flexDirection: 'row', alignItems: 'center', gap: spacing.sm, backgroundColor: colors.surface, padding: spacing.md, borderRadius: radii.md, borderWidth: 1, borderColor: colors.border }, missionIcon: { width: 36, height: 36, borderRadius: 18, backgroundColor: '#382C15', alignItems: 'center', justifyContent: 'center' }, missionBody: { flex: 1, gap: 6 }, missionTitle: { ...typography.body, color: colors.text, fontWeight: '700' }, missionProgress: { ...typography.caption, color: colors.textFaint, letterSpacing: 0 }, done: { color: colors.success, fontSize: 21, fontWeight: '800' }, quote: { marginTop: spacing.xl, padding: spacing.lg, borderLeftWidth: 2, borderLeftColor: colors.red, backgroundColor: colors.surface }, quoteMark: { color: colors.gold, fontSize: 32, lineHeight: 22 }, quoteText: { ...typography.body, color: colors.text, fontWeight: '700', fontStyle: 'italic', marginTop: 4 }, quoteTranslation: { ...typography.caption, color: colors.textFaint, marginTop: 6, letterSpacing: 0 } });
